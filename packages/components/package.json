{
  "name": "@lerna-ws-test/components",
  "version": "1.0.2",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "peerDependencies": {
    "react": "~17.0.0"
  },
  "dependencies": {
    "@lerna-ws-test/foo": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "~17.0.0",
    "react": "~17.0.0",
    "react-dom": "~17.0.0",
    "rimraf": "~3.0.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
