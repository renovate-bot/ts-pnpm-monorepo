{
  "name": "@lerna-ws-test/foo",
  "version": "1.0.2",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "typescript": "~4.6.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
